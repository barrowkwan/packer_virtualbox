# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|

  config.vm.hostname = "vagrant-centos72"
  config.vm.box = "barrowkwan/centos72"
  config.vm.box_url = "box/virtualbox-centos-7.2.box"
  config.ssh.private_key_path = "sshkey/virtualbox_sshkey"

end
