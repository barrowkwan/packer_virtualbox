# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|

  config.vm.hostname = "vagrant-ubuntu1404"
  config.vm.box = "barrowkwan/ubuntu14.04"
  config.vm.box_url = "virtualbox-ubuntu-14.04.box"
  config.ssh.private_key_path = "sshkey/virtualbox_sshkey"

end
